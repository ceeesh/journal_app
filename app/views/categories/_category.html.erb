<div id="<%= dom_id category %>">
  <h2 class="mb-5 ">
    <strong>Category:</strong>
    <%= category.title %>
  </h2>

  <% category.tasks.each do |task|%>
    <p>
    <%= task.title %> | <%= link_to "View Task", task, class: "text-decoration-none"%>
    </p>
  <% end %>



</div>
